#!/bin/bash

# Author: Jim Conner <(snafu dot(.) x at(@) gee mail dot(.) commercial>
read -p 'You should probably kill your browser(s) before completing this... (press any key) ' -r _

if sudo kill -9 $(pgrep ifdreader) $(pgrep -a ctkd); then
  echo 'Now you you need to unplug/plug in your reader.'
fi

